# Los source maps de producción duplican el trabajo del build y no se usan
# en un sitio estático publicado. Ponlo en true si necesitas depurar el build.
GENERATE_SOURCEMAP=false

# Evita que `npm start` abra una pestaña nueva del navegador en cada arranque.
BROWSER=none
